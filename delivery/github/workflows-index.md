# GitHub Workflows Index

| Workflow | File | Trigger | Purpose |
|---|---|---|---|
| validate-docs | `.github/workflows/validate-docs.yml` | PR to main | markdownlint + link check + PRD link validator |

_Add rows as workflows are introduced (test, build, deploy)._
